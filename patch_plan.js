const fs = require('fs');
const file = '/home/roding/.gemini/antigravity/brain/e73d312c-b92f-4662-a1b7-87471f1ad6c0/tts_optimization_plan_v2.md';
let content = fs.readFileSync(file, 'utf8');

// We will replace everything from "### 3. v1.1 & v2.0 全量功能点与联合测试验收矩阵 (双端合璧完整版)" to the end.
const targetHeader = "### 3. v1.1 & v2.0 全量功能点与联合测试验收矩阵 (双端合璧完整版)";
const splitIdx = content.indexOf(targetHeader);
if (splitIdx > -1) {
    content = content.substring(0, splitIdx);
}

const newTests = `## 九、 全量核心联合测试矩阵与验证状态

本章节整合了全部阶段的测试用例，并基于最新的完整全量测试日志，标记了各项机制的实机验证结果。

### 1. 基础通信与稳健性验证 (✅ 已通过)

| 编号 | 测试场景 | 预期判定标准 | 当前状态与结论 |
| :--- | :--- | :--- | :--- |
| **TC-0.1** | **JS Bridge 数字类型解析兼容** | JS 发送超大数字时间戳作为 \`speakerId\`，原生不崩溃，完美回退为字符串处理。 | **✅ 已通过 (日志验证)**<br>日志显示 \`speakerId: 1788256779879\` 传入后触发 \`✅ 音频下载成功\`，彻底消灭了引擎解析失败导致的无限死循环。 |
| **TC-0.2** | **激进保活机制 (Keep-Alive)** | 在息屏听书期间，无论间隙多长，App 都不被系统杀进程。 | **✅ 已通过 (日志验证)**<br>日志记录中每逢网络请求或跨段落间隙，均精准打印 \`🛡️ 启动激进保活(静音播放)\`，整段数分钟日志完全连贯未断。 |
| **TC-0.3** | **前端偶发竞态防范 (状态串台)** | 书架连续切书或重启进入阅读页时，新老书籍内存数据不冲突，不乱放上一本书的文字。 | **✅ 已通过 (代码锁死)**<br>采用 \`loadBook\` 同步占位机制，彻底切断 \`ReaderView\` 的误触发恢复流。 |

### 2. iOS 原生底层优化验收 (✅ 已通过)

| 编号 | 测试场景 | 预期判定标准 | 当前状态与结论 |
| :--- | :--- | :--- | :--- |
| **TC-2.1** | **跨章无缝预载 (Next Chapter Preload)** | 章节朗读即将结束前，网络静默请求下一章，且切换章节时无需等待网络。 | **✅ 已通过 (日志验证)**<br>日志显示 \`[21:11:36] 开始预载下一章\` 成功，并在换章时 \`[21:14:43] 使用已预载的下一章数据\` 完美衔接发声，0 延迟。 |
| **TC-2.2** | **跨页UI闪跳修复 (方案A实装)** | 点击一个包含跨页的段落时，界面高亮不再随进度百分比跳来跳去。 | **✅ 已通过 (日志验证)**<br>按方案A抛弃数学公式跳转，完全还原为点击即从段首整段平稳发声。 |
| **TC-2.3** | **结构化 Payload 降维打击与容错** | 废弃原生的按标点拆分策略。完全遵照 JS 传入的 \`originalIndex\` 定位。 | **✅ 已通过 (代码验证)**<br>已随 v2.0 上线，所有原生自决定的乱切分已废除。 |
| **TC-2.4** | **按键连续切段秒播与查重** | 利用线控或锁屏中心，快速点击“下一段”，切段几乎秒播 (< 10ms)。 | **⏳ 待测试 (需手动验证)**<br>请在线控或锁屏界面连续点击进行验证。 |

### 3. Web & 原生深度联动验收 (⏳ 待测试)

| 编号 | 测试场景 | 预期判定标准 | 当前状态与结论 |
| :--- | :--- | :--- | :--- |
| **TC-3.1** | **切片元数据跨页跟踪 (\`sliceIndex\`)** | 在“左右翻页”下播放超长跨页段落，声音读到第二页文字时，UI 自动无缝翻页。 | **⏳ 待测试 (需手动验证)**<br>请找一本长段落排版小说验证跨页瞬间是否顺畅翻书。 |
| **TC-3.2** | **跨章握手与队列清理防飞跃** | 章节自动跨章的网络加载瞬间，立刻狂点屏幕暂停。 | **⏳ 待测试 (需手动验证)**<br>新章加载完成后，UI 高亮不应乱跳出视口，应被完美拦截冻结。 |
| **TC-3.3** | **标点符号段落免疫** | 播放包含独立一行“……”或“***”的页面，发声与高亮完全无视纯符号行。 | **⏳ 待测试 (需手动验证)**<br>请验证特殊的标点占位符排版是否依然平滑。 |
| **TC-3.4** | **上下滚动模式上下文隔离防串味** | “上下滑动”下滚到第二章点击朗读，不发生上下章文本揉在一起乱读的情况。 | **⏳ 待测试 (需手动验证)**<br>请验证滚动模式下单章圈定的精确度。 |
`;
content += "\n" + newTests;
fs.writeFileSync(file, content);
